---
phase: 02-offline-write-queue
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/src/main/java/com/wenubey/wenucommerce/core/connectivity/PendingSyncViewModel.kt
  - app/src/main/java/com/wenubey/wenucommerce/core/connectivity/PendingSyncBanner.kt
  - app/src/main/java/com/wenubey/wenucommerce/core/connectivity/OfflineConnectivityBanner.kt
  - app/src/main/java/com/wenubey/wenucommerce/core/connectivity/ConnectivityViewModel.kt
  - app/src/main/java/com/wenubey/wenucommerce/queue_management/QueueManagementScreen.kt
  - app/src/main/java/com/wenubey/wenucommerce/queue_management/QueueManagementViewModel.kt
  - app/src/main/java/com/wenubey/wenucommerce/navigation/AppNavigationObjects.kt
  - app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt
  - app/src/main/java/com/wenubey/wenucommerce/di/ViewmodelModule.kt
  - app/src/main/java/com/wenubey/wenucommerce/di/PreferencesModule.kt
  - app/src/main/res/values/strings.xml
autonomous: true
requirements:
  - SYNC-04

must_haves:
  truths:
    - "User sees a global amber banner showing 'N items pending sync' when offline writes are queued"
    - "When offline AND pending items exist, a single combined banner shows 'Offline -- N items pending sync'"
    - "Banner is dismissible; reappears only when pending count increases above dismissed count"
    - "Banner shows a sync-in-progress animation when WorkManager SyncWorker is actively running"
    - "Tapping the banner navigates to a full-screen queue management screen"
    - "Queue management screen shows each operation with type + status and allows retry/discard on failed items"
    - "Banner disappears silently when pending count reaches zero after successful sync"
  artifacts:
    - path: "app/src/main/java/com/wenubey/wenucommerce/core/connectivity/PendingSyncViewModel.kt"
      provides: "ViewModel combining offline state, pending count, dismiss state, sync-in-progress"
      contains: "class PendingSyncViewModel"
    - path: "app/src/main/java/com/wenubey/wenucommerce/core/connectivity/PendingSyncBanner.kt"
      provides: "Merged offline + pending sync banner composable"
      contains: "fun PendingSyncBanner"
    - path: "app/src/main/java/com/wenubey/wenucommerce/queue_management/QueueManagementScreen.kt"
      provides: "Full-screen queue list with retry/discard actions"
      contains: "fun QueueManagementScreen"
    - path: "app/src/main/java/com/wenubey/wenucommerce/queue_management/QueueManagementViewModel.kt"
      provides: "ViewModel observing all operations, retry/discard actions"
      contains: "class QueueManagementViewModel"
  key_links:
    - from: "app/src/main/java/com/wenubey/wenucommerce/core/connectivity/PendingSyncViewModel.kt"
      to: "PendingOperationDao.observePendingCount()"
      via: "Koin-injected DAO"
      pattern: "observePendingCount"
    - from: "app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt"
      to: "PendingSyncBanner"
      via: "AnimatedVisibility overlay replacing OfflineConnectivityBanner"
      pattern: "PendingSyncBanner"
    - from: "app/src/main/java/com/wenubey/wenucommerce/queue_management/QueueManagementViewModel.kt"
      to: "PendingOperationDao"
      via: "Koin-injected DAO for retry/discard"
      pattern: "pendingOperationDao"
    - from: "app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt"
      to: "QueueManagement route"
      via: "Banner tap navigates to queue screen"
      pattern: "QueueManagement"
---

<objective>
Create the pending-sync UI layer: a merged offline + pending-sync banner that replaces the standalone offline banner, a queue management screen for viewing and managing pending operations, and the "saved locally" snackbar feedback for offline writes.

Purpose: Make the offline write queue visible and manageable to users. Without this UI, queued writes would be invisible and unmanageable, violating the SYNC-04 requirement.

Output: PendingSyncViewModel, PendingSyncBanner composable (merging offline + pending states), QueueManagementScreen with retry/discard, navigation wiring, DataStore-backed dismiss state.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-offline-write-queue/02-RESEARCH.md
@.planning/phases/02-offline-write-queue/02-01-SUMMARY.md
@.planning/phases/01-room-foundation/01-03-SUMMARY.md
@.planning/phases/01-room-foundation/01-04-SUMMARY.md

Key existing files to reference:
@app/src/main/java/com/wenubey/wenucommerce/core/connectivity/OfflineConnectivityBanner.kt (WILL BE REPLACED)
@app/src/main/java/com/wenubey/wenucommerce/core/connectivity/ConnectivityViewModel.kt (WILL BE REPLACED by PendingSyncViewModel)
@app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt
@app/src/main/java/com/wenubey/wenucommerce/di/ViewmodelModule.kt
@app/src/main/java/com/wenubey/wenucommerce/di/PreferencesModule.kt
@app/src/main/java/com/wenubey/wenucommerce/navigation/AppNavigationObjects.kt
@data/src/main/java/com/wenubey/data/local/dao/PendingOperationDao.kt (from 02-01)
@data/src/main/java/com/wenubey/data/local/entity/PendingOperationEntity.kt (from 02-01)
@data/src/main/java/com/wenubey/data/worker/SyncWorker.kt (from 02-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PendingSyncViewModel and merged PendingSyncBanner; replace offline banner in MainActivity</name>
  <files>
    app/src/main/java/com/wenubey/wenucommerce/core/connectivity/PendingSyncViewModel.kt
    app/src/main/java/com/wenubey/wenucommerce/core/connectivity/PendingSyncBanner.kt
    app/src/main/java/com/wenubey/wenucommerce/core/connectivity/ConnectivityViewModel.kt
    app/src/main/java/com/wenubey/wenucommerce/core/connectivity/OfflineConnectivityBanner.kt
    app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt
    app/src/main/java/com/wenubey/wenucommerce/di/ViewmodelModule.kt
    app/src/main/java/com/wenubey/wenucommerce/di/PreferencesModule.kt
    app/src/main/res/values/strings.xml
  </files>
  <action>
    **1. Add a "pendingSync" DataStore in PreferencesModule.kt:**
    - Add a new DataStore: `private val Context.pendingSyncPreferences: DataStore<Preferences> by preferencesDataStore(name = "pendingSyncPreferences")`
    - Add `const val PENDING_SYNC_PREFERENCE_NAME = "pendingSyncPreferences"`
    - Add to the preferencesModule: `single(named("pendingSync")) { androidContext().pendingSyncPreferences }`

    **2. Create PendingSyncViewModel (`core/connectivity/PendingSyncViewModel.kt`):**
    - Constructor params: `connectivityObserver: ConnectivityObserver`, `pendingOperationDao: PendingOperationDao`, `dataStore: DataStore<Preferences>` (qualified with `named("pendingSync")`), `application: Application` (for WorkManager access)
    - Expose `isOnline: StateFlow<Boolean>` from connectivityObserver.isOnline, stateIn with WhileSubscribed(5000), initialValue=true (same pattern as old ConnectivityViewModel)
    - Expose `pendingCount: StateFlow<Int>` from `pendingOperationDao.observePendingCount()`, stateIn with WhileSubscribed(5000), initialValue=0
    - Private `dismissedCountKey = intPreferencesKey("dismissed_pending_count")`
    - Private `dismissedCount: StateFlow<Int>` from `dataStore.data.map { it[dismissedCountKey] ?: 0 }`, stateIn with WhileSubscribed(5000), initialValue=0
    - Expose `shouldShowBanner: StateFlow<Boolean>` = `combine(isOnline, pendingCount, dismissedCount) { online, pending, dismissed -> !online || (pending > 0 && pending > dismissed) }`, stateIn with WhileSubscribed(5000), initialValue=false
    - Expose `isSyncing: StateFlow<Boolean>`: Use `WorkManager.getInstance(application).getWorkInfosForUniqueWorkFlow(SyncWorker.UNIQUE_WORK_NAME)` (Flow-based API available in WorkManager 2.11.x) and map to `workInfos.any { it.state == WorkInfo.State.RUNNING }`, stateIn with WhileSubscribed(5000), initialValue=false
    - `fun dismissBanner()`: Launch in viewModelScope, `dataStore.edit { it[dismissedCountKey] = pendingCount.value }`

    **3. Create PendingSyncBanner (`core/connectivity/PendingSyncBanner.kt`):**
    - Parameters: `isOnline: Boolean`, `pendingCount: Int`, `isSyncing: Boolean`, `onDismiss: () -> Unit`, `onTap: () -> Unit`, `modifier: Modifier = Modifier`
    - Layout: Surface (amber Color(0xFFFFC107), fillMaxWidth, statusBarsPadding, clickable { onTap() }) > Row (padding 16/10, spacedBy 8)
    - Left icon: If `isSyncing` -> `CircularProgressIndicator(Modifier.size(20.dp), color=White, strokeWidth=2.dp)`. Else if `!isOnline` -> `Icons.Default.WifiOff`. Else -> `Icons.Default.CloudUpload`. All tinted White.
    - Text (weight 1f, White, bodyMedium bold): Use `when` block:
      - `!isOnline && pendingCount > 0` -> "Offline -- $pendingCount items pending sync"
      - `!isOnline` -> stringResource(R.string.no_internet_connection)
      - `isSyncing` -> "Syncing $pendingCount items..."
      - else -> "$pendingCount items pending sync"
    - Right: `IconButton(onClick = onDismiss)` with `Icons.Default.Close` (White). Only show the close button when pendingCount > 0 (offline-only banner should NOT be dismissible, per original behavior).
    - Add string resources to strings.xml: `pending_sync_count` = "%d items pending sync", `offline_pending_sync` = "Offline -- %d items pending sync", `syncing_items` = "Syncing %d items..."

    **4. Replace ConnectivityViewModel with PendingSyncViewModel:**
    - The old `ConnectivityViewModel` only exposed `isOnline`. The new `PendingSyncViewModel` exposes `isOnline` plus pending sync state. ConnectivityViewModel.kt can be left in place (other ViewModels like CustomerHomeViewModel reference ConnectivityObserver directly), but remove it from ViewmodelModule.kt if it was registered there.
    - Check if ConnectivityViewModel is used elsewhere. If CustomerHomeViewModel creates its own isOnline from ConnectivityObserver, ConnectivityViewModel is only used in MainActivity. In that case, remove the ConnectivityViewModel import from ViewmodelModule.kt and add PendingSyncViewModel instead.
    - In ViewmodelModule.kt: Replace `viewModelOf(::ConnectivityViewModel)` with `viewModelOf(::PendingSyncViewModel)`. The PendingSyncViewModel needs the named DataStore qualifier, so use: `viewModel { PendingSyncViewModel(get(), get(), get(named("pendingSync")), get()) }` instead of viewModelOf.
    - Add `viewModelOf(::QueueManagementViewModel)` (for Task 2 — add it here to avoid modifying ViewmodelModule twice).

    **5. Update MainActivity.kt to use PendingSyncBanner:**
    - Remove the `ConnectivityViewModel` / `connectivityVm` code
    - Add: `val pendingSyncVm: PendingSyncViewModel = koinViewModel()`
    - Collect: `isOnline`, `pendingCount`, `shouldShowBanner`, `isSyncing` via `collectAsStateWithLifecycle()`
    - Replace the existing `AnimatedVisibility(!isOnline) { OfflineConnectivityBanner() }` block with:
      ```kotlin
      AnimatedVisibility(
          visible = shouldShowBanner,
          modifier = Modifier.align(Alignment.TopCenter),
          enter = slideInVertically(initialOffsetY = { -it }),
          exit = slideOutVertically(targetOffsetY = { -it }),
      ) {
          PendingSyncBanner(
              isOnline = isOnline,
              pendingCount = pendingCount,
              isSyncing = isSyncing,
              onDismiss = { pendingSyncVm.dismissBanner() },
              onTap = { navController.navigate(QueueManagement) },
          )
      }
      ```
    - Keep the existing sync failure snackbar LaunchedEffect unchanged.
    - Import `QueueManagement` from navigation objects (will be created in Task 2).

    **6. Keep OfflineConnectivityBanner.kt file intact** but it is now unused by MainActivity. It may still be useful for other screens in the future or can be deleted. Do NOT delete it — leave it as-is to avoid breaking any potential references. Mark it with a `// Superseded by PendingSyncBanner — retained for potential reuse` comment at the top.
  </action>
  <verify>
    Run `./gradlew :app:compileDebugKotlin` — compiles. Verify PendingSyncViewModel is registered in ViewmodelModule. Verify MainActivity uses PendingSyncBanner instead of OfflineConnectivityBanner. Verify PreferencesModule exports the pendingSync DataStore.
  </verify>
  <done>
    PendingSyncViewModel exists with isOnline, pendingCount, shouldShowBanner (with dismiss logic), and isSyncing states. PendingSyncBanner shows merged offline + pending state with conditional text, icons, sync animation, and dismiss button. MainActivity uses PendingSyncBanner in place of OfflineConnectivityBanner. DataStore persists the dismissed count across restarts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create QueueManagementScreen, QueueManagementViewModel, and navigation wiring</name>
  <files>
    app/src/main/java/com/wenubey/wenucommerce/queue_management/QueueManagementScreen.kt
    app/src/main/java/com/wenubey/wenucommerce/queue_management/QueueManagementViewModel.kt
    app/src/main/java/com/wenubey/wenucommerce/navigation/AppNavigationObjects.kt
    app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt
    app/src/main/res/values/strings.xml
  </files>
  <action>
    **1. Add QueueManagement navigation route (`AppNavigationObjects.kt`):**
    - Add `@Serializable data object QueueManagement` at the end of the file
    - This follows the existing pattern — all route objects are @Serializable data objects

    **2. Create QueueManagementViewModel (`queue_management/QueueManagementViewModel.kt`):**
    - Constructor params: `private val pendingOperationDao: PendingOperationDao`, `private val dispatcherProvider: DispatcherProvider`, `private val application: Application`
    - `val operations: StateFlow<List<PendingOperationUiModel>>` = `pendingOperationDao.observeAllOperations().map { entities -> entities.map { it.toUiModel() } }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), emptyList())`
    - Define `PendingOperationUiModel` data class in same file: `val id: Long`, `val displayName: String`, `val statusText: String`, `val status: OperationStatus`, `val createdAt: String`
    - Private extension `PendingOperationEntity.toUiModel()`:
      - `displayName`: Map OperationType to user-friendly name using `runCatching { OperationType.valueOf(operationType) }.getOrNull()`:
        - ADD_TO_CART -> "Cart update"
        - UPDATE_CART_QUANTITY -> "Cart update"
        - REMOVE_FROM_CART -> "Cart update"
        - UPDATE_PROFILE -> "Profile update"
        - SUBMIT_REVIEW -> "Review submission"
        - null (unknown) -> "Unknown operation"
      - `statusText`: Map OperationStatus: PENDING -> "Pending", IN_PROGRESS -> "Syncing...", FAILED -> "Failed"
      - `status`: `runCatching { OperationStatus.valueOf(status) }.getOrDefault(OperationStatus.PENDING)`
      - `createdAt`: Format the ISO 8601 string to a user-friendly relative or absolute time. Use `java.time.Instant.parse(createdAt)` and format with a simple pattern (e.g., "Today 14:30" or "Feb 20, 14:30"). Use `runCatching` with fallback to raw string.
    - `fun retryOperation(id: Long)`: Launch in `viewModelScope` with `dispatcherProvider.io()`:
      - `pendingOperationDao.updateStatus(id, OperationStatus.PENDING.name, java.time.Instant.now().toString())`
      - `SyncWorker.enqueue(application)` to trigger processing
    - `fun discardOperation(id: Long)`: Launch in `viewModelScope` with `dispatcherProvider.io()`:
      - `pendingOperationDao.deleteById(id)`

    **3. Create QueueManagementScreen (`queue_management/QueueManagementScreen.kt`):**
    - Parameters: `viewModel: QueueManagementViewModel = koinViewModel()`, `onNavigateBack: () -> Unit`
    - Collect `operations` via `collectAsStateWithLifecycle()`
    - Layout: `Scaffold` with `TopAppBar`:
      - Title: "Pending Sync Queue" (add string resource)
      - NavigationIcon: `IconButton(onClick = onNavigateBack)` with `Icons.AutoMirrored.Filled.ArrowBack`
    - Body: If `operations.isEmpty()`, show a centered "No pending operations" text (add string resource).
    - Otherwise: `LazyColumn` with `contentPadding(16.dp)`, `verticalArrangement = spacedBy(8.dp)`, `items(operations, key = { it.id })`:
      - Each item: `Card(Modifier.fillMaxWidth())` containing a `Row(Modifier.padding(16.dp))`:
        - Left `Column(Modifier.weight(1f))`:
          - `Text(operation.displayName, style = titleMedium)`
          - `Text("${operation.statusText} - ${operation.createdAt}", style = bodySmall, color = onSurfaceVariant)`
          - If `operation.status == FAILED`, show a small error text in `MaterialTheme.colorScheme.error`
        - Right (only if `operation.status == OperationStatus.FAILED`): `Row(spacedBy 4.dp)`:
          - `IconButton(onClick = { viewModel.retryOperation(it.id) })` with `Icons.Default.Refresh` (contentDescription "Retry")
          - `IconButton(onClick = { viewModel.discardOperation(it.id) })` with `Icons.Default.Delete` (contentDescription "Discard")

    **4. Wire QueueManagement route in MainActivity.kt:**
    - The `RootNavigationGraph` composable function should contain the route. However, the queue management screen is not role-specific — it should be accessible from any navigation context.
    - Find where `RootNavigationGraph` or the main `NavHost` is defined. Add a `composable<QueueManagement>` route:
      ```kotlin
      composable<QueueManagement> {
          QueueManagementScreen(
              onNavigateBack = { navController.popBackStack() }
          )
      }
      ```
    - If `RootNavigationGraph` is a separate file that builds the NavHost, add the composable there. If the NavHost is in a different file, find the correct location and add it.
    - The banner's `onTap = { navController.navigate(QueueManagement) }` in MainActivity (from Task 1) already navigates here.

    **5. Add string resources (`strings.xml`):**
    - `pending_sync_queue_title` = "Pending Sync Queue"
    - `no_pending_operations` = "No pending operations"
    - `saved_locally_snackbar` = "Saved locally -- will sync when online"
    - `some_items_failed_to_sync` = "Some items failed to sync"
    - Any other strings used in the banner text (if not already hardcoded — prefer string resources for user-visible text)

    **NOTE on offline-write snackbar:** The "Saved locally -- will sync when online" snackbar (per locked decision) will be triggered by repositories when they enqueue a pending operation while offline. The snackbar mechanism already exists in MainActivity (SnackbarHostState). To enable this, add a new `SyncEvent` subclass `SyncEvent.OfflineWriteQueued` to `SyncManager.kt` (in the data module). Then in MainActivity's existing LaunchedEffect, add a handler:
    ```kotlin
    is SyncEvent.OfflineWriteQueued -> {
        snackbarHostState.showSnackbar(
            message = savedLocallyMessage,
            duration = SnackbarDuration.Short,
        )
    }
    ```
    Also add `SyncEvent.SyncPartialFailure(val message: String)` for the "Some items failed to sync" error snackbar. Both events are emitted by SyncManager when needed (wired in future phases when repositories start using the write queue). For now, declare the event types and the collection handler so the infrastructure is ready.
    - Modify `data/src/main/java/com/wenubey/data/local/SyncManager.kt`: Add `data object OfflineWriteQueued : SyncEvent` and `data class SyncPartialFailure(val message: String) : SyncEvent` to the sealed interface.
  </action>
  <verify>
    Run `./gradlew assembleDebug` — full build succeeds. Verify QueueManagement route exists in AppNavigationObjects.kt. Verify QueueManagementScreen composable exists with LazyColumn and retry/discard actions. Verify the composable<QueueManagement> route is wired in the navigation graph. Verify SyncEvent has OfflineWriteQueued and SyncPartialFailure subclasses.
  </verify>
  <done>
    QueueManagementScreen exists showing pending operations with type + status, retry and discard buttons on failed items. Navigation from banner tap to queue screen works. SyncEvent.OfflineWriteQueued and SyncEvent.SyncPartialFailure are declared and handled in MainActivity's snackbar collection. All string resources are defined. The full pending-sync UI infrastructure is complete and ready for repositories to start enqueuing operations.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` passes — all new code compiles
2. PendingSyncBanner renders correctly in three states: offline only, pending only, offline + pending combined
3. Banner dismiss persists across app restarts (DataStore-backed)
4. Banner reappears when new writes are queued (pending count exceeds dismissed count)
5. QueueManagementScreen renders empty state when no operations exist
6. QueueManagementScreen shows retry/discard buttons only on FAILED operations
7. Navigation from banner tap to QueueManagementScreen works
8. SyncEvent sealed interface has OfflineWriteQueued and SyncPartialFailure subtypes
</verification>

<success_criteria>
- PendingSyncBanner replaces OfflineConnectivityBanner in MainActivity
- Banner shows correct text for offline-only, pending-only, and combined states
- Banner is dismissible with DataStore persistence; reappears on count increase
- Sync-in-progress animation shows CircularProgressIndicator when worker is running
- QueueManagementScreen accessible via banner tap with retry/discard on failed items
- SyncEvent extended with OfflineWriteQueued and SyncPartialFailure
- Full assembleDebug passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-write-queue/02-02-SUMMARY.md`
</output>
