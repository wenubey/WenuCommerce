---
phase: 02-offline-write-queue
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt
autonomous: true
requirements: [SYNC-03, SYNC-04]
gap_closure: true

must_haves:
  truths:
    - "Tapping the offline banner navigates to a screen titled 'Pending Sync Queue' with a back arrow visibly in the top bar"
    - "The amber banner is not visible while the QueueManagementScreen is active"
    - "Returning from QueueManagementScreen restores the banner if conditions still require it"
  artifacts:
    - path: "app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt"
      provides: "Route-aware banner suppression"
      contains: "currentDestination?.hasRoute"
  key_links:
    - from: "MainActivity.kt AnimatedVisibility(visible = shouldShowBanner)"
      to: "QueueManagement route guard"
      via: "navController.currentBackStackEntryAsState().value?.destination"
      pattern: "currentDestination.*hasRoute.*QueueManagement"
---

<objective>
Close UAT gap: the PendingSyncBanner overlay in MainActivity covers the TopAppBar of QueueManagementScreen.

Purpose: The amber banner is rendered as a persistent floating overlay at Alignment.TopCenter in MainActivity. Because shouldShowBanner remains true while offline (the condition that triggered navigation to QueueManagementScreen), the banner surface — positioned with statusBarsPadding() — occupies the exact same vertical space as QueueManagementScreen's TopAppBar, making the back arrow and title invisible to the user.

Output: MainActivity suppresses the banner overlay when the active destination is QueueManagement. The banner reappears automatically when the user navigates back.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-offline-write-queue/02-02-SUMMARY.md
@.planning/debug/02-missing-topbar.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suppress banner overlay on QueueManagement route</name>
  <files>app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt</files>
  <action>
    In MainActivity.kt, read the current back-stack destination and derive a boolean that is true when the active destination is QueueManagement.

    Steps:

    1. Add this import (already present or add below existing navigation imports):
       ```
       import androidx.navigation.compose.currentBackStackEntryAsState
       ```

    2. Inside the setContent { ... } block, after `navController = rememberNavController()`, add:
       ```kotlin
       val currentBackStackEntry by navController.currentBackStackEntryAsState()
       val isOnQueueManagementScreen = currentBackStackEntry
           ?.destination
           ?.hasRoute(QueueManagement::class) == true
       ```
       `QueueManagement` is already imported at line 32. `hasRoute` requires the KClass overload — use `QueueManagement::class`.

    3. Modify the `AnimatedVisibility` `visible` parameter from:
       ```kotlin
       visible = shouldShowBanner,
       ```
       to:
       ```kotlin
       visible = shouldShowBanner && !isOnQueueManagementScreen,
       ```

    Do NOT modify the `PendingSyncBanner` composable itself. Do NOT change the `PendingSyncViewModel` or the `dismissBanner()` call. Do NOT touch `shouldShowBanner` logic. The only change is the `visible` condition on the existing `AnimatedVisibility`.

    The `slideOutVertically` exit animation is already configured — when the user taps the banner and navigates to QueueManagementScreen, the banner will slide up and out automatically as `isOnQueueManagementScreen` becomes true. When the user presses back, `isOnQueueManagementScreen` becomes false and the banner slides back in if conditions still apply.
  </action>
  <verify>
    Build the app: `./gradlew :app:assembleDebug` — must succeed with no errors.

    Manual smoke test (run on device or emulator):
    1. Enable airplane mode.
    2. Launch the app. Confirm amber banner appears at top.
    3. Tap the banner. Confirm navigation to "Pending Sync Queue" screen.
    4. Confirm TopAppBar title "Pending Sync Queue" is visible.
    5. Confirm back arrow (ArrowBack icon) is visible in the top-left.
    6. Tap the back arrow. Confirm return to previous screen.
    7. Confirm amber banner reappears after returning.
  </verify>
  <done>
    - Build passes with no errors or warnings introduced by this change.
    - TopAppBar title and back arrow are visible on QueueManagementScreen while offline.
    - Banner is absent while QueueManagementScreen is active.
    - Banner reappears when navigating back to any other screen while still offline.
  </done>
</task>

</tasks>

<verification>
Run `./gradlew :app:assembleDebug` and confirm a clean build.

The fix is minimal and surgical: one boolean derived from navController state, one condition added to AnimatedVisibility. No new dependencies, no architecture changes, no ViewModel changes.
</verification>

<success_criteria>
UAT Test 4 passes: Tapping the offline banner navigates to a screen titled "Pending Sync Queue" with a back arrow in the top bar. Both the title and back arrow are visible. All other UAT tests (1-3, 5-7) remain passing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-write-queue/02-03-SUMMARY.md` following the summary template.
</output>
