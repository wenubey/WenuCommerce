---
phase: 07-reviews-ratings
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailState.kt
  - app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailAction.kt
  - app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailViewModel.kt
  - app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailScreen.kt
  - app/src/main/java/com/wenubey/wenucommerce/customer/customer_reviews/WriteReviewScreen.kt
  - app/src/main/java/com/wenubey/wenucommerce/customer/customer_reviews/StarRatingSelector.kt
  - app/src/main/java/com/wenubey/wenucommerce/core/components/VerifiedPurchaseBadge.kt
  - app/src/main/java/com/wenubey/wenucommerce/core/components/ReviewCard.kt
  - app/src/main/java/com/wenubey/wenucommerce/navigation/AppNavigationObjects.kt
  - app/src/main/java/com/wenubey/wenucommerce/navigation/AppNavigation.kt
  - app/src/test/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailViewModelTest.kt
  - app/src/androidTest/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailScreenTest.kt
autonomous: true
requirements: [REVW-01, REVW-04, REVW-05, REVW-06]
user_setup: []

must_haves:
  truths:
    - "A customer with a DELIVERED order for the product sees a 'Write a Review' affordance; without one it is hidden (D-07)"
    - "If the customer already reviewed, the affordance reads 'Edit Your Review' and opens the form pre-filled (D-05)"
    - "The reviews section shows an aggregate rating header (average + count) above the list"
    - "Each review card shows a 'Verified Purchase' badge when isVerifiedPurchase is true (REVW-05)"
    - "A segmented toggle switches the list between 'Most recent' (default) and 'Highest rated', ties broken by recency (REVW-06)"
    - "Submitting a 1-5 star review with optional text invokes ProductReviewRepository.submitReview and pops back with a snackbar (REVW-01)"
    - "The Helpful action invokes markReviewHelpful and disables optimistically (D-04)"
  artifacts:
    - path: "app/src/main/java/com/wenubey/wenucommerce/customer/customer_reviews/WriteReviewScreen.kt"
      provides: "Full-screen write/edit review form with StarRatingSelector, title/body fields, submit spinner"
      contains: "WriteReviewScreen"
    - path: "app/src/main/java/com/wenubey/wenucommerce/core/components/VerifiedPurchaseBadge.kt"
      provides: "Verified Purchase badge composable"
      contains: "Verified Purchase"
    - path: "app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailViewModel.kt"
      provides: "hasDeliveredOrder gate, existingReview pre-fill, sort logic, submit/helpful handlers"
      contains: "checkDeliveredOrderStatus"
    - path: "app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailScreen.kt"
      provides: "AggregateRatingHeader, ReviewSortToggle, write-review affordance, badge-enhanced cards"
      contains: "SingleChoiceSegmentedButtonRow"
  key_links:
    - from: "CustomerProductDetailViewModel"
      to: "SellerOrderDao.getByUserAndStatus"
      via: "delivered-order eligibility gate"
      pattern: "getByUserAndStatus"
    - from: "CustomerProductDetailViewModel"
      to: "ProductReviewRepository.submitReview"
      via: "SubmitReview action handler"
      pattern: "reviewRepository\\.submitReview"
    - from: "CustomerProductDetailScreen"
      to: "WriteReview route"
      via: "navigation on affordance tap"
      pattern: "WriteReview"
---

<objective>
Surface the full reviews UX on the customer product detail screen: an aggregate rating header, a segmented sort toggle (Most recent default / Highest rated, tie-break recency — REVW-06), individual review cards with a Verified Purchase badge (REVW-05) and a Helpful action (D-04), and a "Write a Review" / "Edit Your Review" affordance gated to customers with a DELIVERED order (D-07), opening a pre-fillable form (D-05) that submits a 1-5 star rating with optional text (REVW-01).

Purpose: Turn the server foundation from 07-01 into the customer-facing review experience per the UI-SPEC contract.
Output: Extended state/action/ViewModel/screen, new WriteReviewScreen + StarRatingSelector + VerifiedPurchaseBadge + extracted ReviewCard, WriteReview nav route, ViewModel + Compose tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reviews-ratings/07-CONTEXT.md
@.planning/phases/07-reviews-ratings/07-PATTERNS.md
@.planning/phases/07-reviews-ratings/07-UI-SPEC.md
@.planning/phases/07-reviews-ratings/07-VALIDATION.md
@./CLAUDE.md

<interfaces>
<!-- Contracts from 07-01 (wave 1) and existing code. Use directly. -->

From domain/.../repository/ProductReviewRepository.kt (after 07-01):
```kotlin
fun observeReviewsForProduct(productId: String): Flow<List<ProductReview>>   // Room-first
suspend fun submitReview(review: ProductReview): Result<ProductReview>
suspend fun markReviewHelpful(productId: String, reviewId: String): Result<Unit>
suspend fun getMyReviewForProduct(productId: String): Result<ProductReview?>  // NEW in 07-01 — pre-fill
```

From data/.../dao/SellerOrderDao.kt (added in 07-01):
```kotlin
suspend fun getByUserAndStatus(userId: String, status: String): List<SellerOrderEntity>
// SellerOrderEntity.itemsJson is a JSON array of items each with a productId field.
// Decode with Json { ignoreUnknownKeys = true } to a list and check .any { it.productId == productId }.
```

ProductReview fields (canonical): id, productId, reviewerId, reviewerName, reviewerPhotoUrl, purchaseId, rating(Int 1-5), title, body, isVerifiedPurchase, helpfulCount, isVisible, createdAt (epoch-millis string — sortable descending as string), updatedAt.

Current CustomerProductDetailState (lines 7-23) fields to KEEP: product, reviews, isLoading, isLoadingReviews, errorMessage, cartMessage, showLoginPrompt, isWishlisted.

Current CustomerProductDetailViewModel already: injects ProductReviewRepository + AuthRepository, calls observeReviewsForProduct in init, handles OnMarkReviewHelpful. It must gain injection of SellerOrderDao (add to constructor + Koin) for the delivered gate.

Nav pattern (AppNavigationObjects.kt): `@Serializable data class CustomerProductDetail(val productId: String)` (line 57). AppNavigation.kt hosts composable<Route> destinations.

UI-SPEC component IDs: C-02 StarRatingSelector, C-03 VerifiedPurchaseBadge, C-04 AggregateRatingHeader, C-05 ReviewSortToggle, C-06 ReviewCard, C-07 WriteReviewForm, C-08 WriteReviewAffordance. Copywriting + accessibility + color/spacing/typography are in 07-UI-SPEC.md — follow exactly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State + Action + ViewModel — delivered-order gate, existingReview pre-fill, sort, submit + helpful handlers (with Turbine tests)</name>
  <read_first>
    - app/.../customer/customer_products/CustomerProductDetailState.kt — existing fields (analog: PATTERNS.md lines 7-23) to append to.
    - app/.../customer/customer_products/CustomerProductDetailAction.kt — existing sealed interface (analog: PATTERNS.md lines 5-17).
    - app/.../customer/customer_products/CustomerProductDetailViewModel.kt — WHOLE file: init block, observeReviews collect (analog lines 112-130), addToCart runCatching handler (analog lines 185-208), existing OnMarkReviewHelpful handling.
    - .planning/phases/07-reviews-ratings/07-PATTERNS.md §"CustomerProductDetailViewModel additions" — checkDeliveredOrderStatus, sortedReviews helper, submitReview handler reference blocks.
    - .planning/phases/07-reviews-ratings/07-UI-SPEC.md §"Interaction Contracts" (sort tie-break, helpful optimistic disable) and §"Screen States".
    - Any existing *ViewModelTest.kt under app/src/test (e.g. a discount or cart VM test) for the MainDispatcherRule + Turbine + StandardTestDispatcher setup and the project's fake-repository style.
  </read_first>
  <behavior>
    CustomerProductDetailViewModelTest (app/src/test, Turbine, MainDispatcherRule, StandardTestDispatcher; fake ProductReviewRepository + fake SellerOrderDao/AuthRepository):
    - hasDeliveredOrder gate: given the fake SellerOrderDao returns a DELIVERED order whose itemsJson contains productId → state.hasDeliveredOrder == true; given none → false (REVW-02 UI affordance / D-07).
    - existingReview pre-fill: given getMyReviewForProduct returns a review → state.existingReview == that review (D-05).
    - sort default: reviews emit unsorted → state.reviews are ordered by createdAt descending (MOST_RECENT default — REVW-06).
    - sort toggle: OnSortOrderChanged(HIGHEST_RATED) → list ordered by rating descending, ties broken by createdAt descending (feed two same-rating reviews with different createdAt; assert recency tie-break).
    - submit success: SubmitReview(rating=4,...) with a fake repo returning success → isSubmittingReview transitions true→false, showReviewForm false, a success message set (REVW-01).
    - submit failure: fake repo returns failure("No delivered order found for this product") → reviewSubmitError set, isSubmittingReview false.
    - helpful optimistic disable: OnMarkReviewHelpful(reviewId) adds reviewId to helpfulVotedIds immediately (D-04) and calls markReviewHelpful.
  </behavior>
  <action>
    Append to CustomerProductDetailState: `hasDeliveredOrder: Boolean = false`, `existingReview: ProductReview? = null`, `reviewSortOrder: ReviewSortOrder = ReviewSortOrder.MOST_RECENT`, `isSubmittingReview: Boolean = false`, `showReviewForm: Boolean = false`, `reviewSubmitError: String? = null`, `isCheckingEligibility: Boolean = false`, `helpfulVotedIds: Set<String> = emptySet()`. Add `enum class ReviewSortOrder { MOST_RECENT, HIGHEST_RATED }` (same file or sibling). Add actions: `OpenReviewForm`, `DismissReviewForm`, `data class SubmitReview(rating, title, body)`, `data class OnSortOrderChanged(order: ReviewSortOrder)`. Add `sellerOrderDao: SellerOrderDao` to the ViewModel constructor (and to its Koin binding in the app DI module). In init, call `checkDeliveredOrderStatus(productId)` and `loadExistingReview(productId)`. checkDeliveredOrderStatus: read `authRepository.currentUser.value?.uuid`, launch on io, `sellerOrderDao.getByUserAndStatus(userId, "DELIVERED")`, decode each itemsJson with `Json { ignoreUnknownKeys = true }` (runCatching→false per entity) and check any item productId == productId; set state.hasDeliveredOrder (Pitfall: use runCatching around decode). loadExistingReview: `reviewRepository.getMyReviewForProduct(productId).getOrNull()` → state.existingReview. Add pure `sortedReviews(list, order)` (MOST_RECENT → sortedByDescending createdAt; HIGHEST_RATED → compareByDescending rating thenByDescending createdAt) and apply it in the observeReviews collect block AND re-apply on OnSortOrderChanged. Add submitReview handler (runCatching → onSuccess: isSubmittingReview=false, showReviewForm=false, cartMessage="Review submitted" or "Review updated" if existingReview!=null; onFailure: reviewSubmitError = message). Extend OnMarkReviewHelpful to add reviewId to helpfulVotedIds optimistically then call markReviewHelpful (ignore already-voted failures). Write CustomerProductDetailViewModelTest per <behavior>.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*CustomerProductDetailViewModelTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:testDebugUnitTest --tests "*CustomerProductDetailViewModelTest"` green (all <behavior> cases).
    - `grep -c "getByUserAndStatus" app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailViewModel.kt` returns 1.
    - `grep -c "ReviewSortOrder.HIGHEST_RATED\|thenByDescending" app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailViewModel.kt` >= 1 (tie-break present).
    - `grep -c "reviewRepository.submitReview\|reviewRepository.getMyReviewForProduct" app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailViewModel.kt` >= 2.
    - No Thread.sleep in the test (grep returns 0).
  </acceptance_criteria>
  <done>State/Action extended; ViewModel gates on delivered order, pre-fills existing review, sorts with recency tie-break, handles submit + helpful; Turbine tests green with no sleeps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VerifiedPurchaseBadge + StarRatingSelector + extracted ReviewCard + WriteReviewScreen + WriteReview nav route</name>
  <read_first>
    - app/.../core/components/OrderStatusBadge.kt — BadgeChip (analog: PATTERNS.md lines 39-53) for the badge container/content-color + RoundedCornerShape(8dp) + padding H10 V4 pattern.
    - app/.../customer/customer_products/CustomerProductDetailScreen.kt — existing private ReviewCard (lines 664-746) to EXTRACT to core/components/ReviewCard.kt; the star row pattern (lines 700-708); CartActionSection spinner-in-button (lines 504-660) for WriteReviewScreen submit; the snackbar LaunchedEffect (lines 83-97).
    - app/.../seller/seller_products/SellerProductCreateScreen.kt — full-screen Scaffold + TopAppBar back-nav pattern for WriteReviewScreen.
    - app/.../navigation/AppNavigationObjects.kt (lines 57, 109) — @Serializable parameterized route pattern; AppNavigation.kt — how composable<Route> destinations are registered and how nav args are read.
    - .planning/phases/07-reviews-ratings/07-UI-SPEC.md §C-02, §C-03, §C-06, §C-07, §"Copywriting Contract", §"Accessibility Contract", §"Color", §"Spacing Scale", §"Typography" — the binding visual/interaction contract.
    - .planning/phases/07-reviews-ratings/07-PATTERNS.md §"VerifiedPurchaseBadge", §"WriteReviewScreen", §"AppNavigationObjects WriteReview route".
  </read_first>
  <action>
    Create VerifiedPurchaseBadge.kt (core/components) per UI-SPEC C-03: stateless, text "Verified Purchase", labelMedium, onPrimaryContainer on primaryContainer background, RoundedCornerShape(8dp), padding horizontal 10dp vertical 4dp. Create StarRatingSelector.kt (customer_reviews) per C-02: 5 IconButtons each 44dp touch target, 32dp icon, Filled.Star (colorScheme.primary) for index<selectedRating else Outlined.Star (onSurfaceVariant), onRatingSelected(N+1), per-button contentDescription "Rate $n out of 5 stars", no animation. Extract the existing private ReviewCard to core/components/ReviewCard.kt and extend per C-06: header Row with 32dp avatar + reviewerName(labelMedium) + StarRatingDisplay(Medium, amber 0xFFFFC107), then Spacer(weight=1f) + VerifiedPurchaseBadge (conditional on isVerifiedPurchase) + relative date (bodySmall, onSurfaceVariant); title(titleSmall, conditional) + body(bodyMedium); Helpful as a TextButton labeled "Helpful ($helpfulCount)" with Outlined.ThumbUp (16dp), disabled when the review id is in helpfulVotedIds (accepts a `hasVoted: Boolean` + `onHelpful: () -> Unit`), contentDescription per C-06/Accessibility. Create WriteReviewScreen.kt (customer_reviews) per C-07: Scaffold + TopAppBar (title "Write a Review" / "Edit Review" when editing) with back nav; LazyColumn (md=16dp horizontal padding) with StarRatingSelector (required), OutlinedTextField Title (max 100, single line, placeholder "Summarise your experience"), OutlinedTextField Review (minLines 4, maxLines 8, max 1000 with "${body.length}/1000" counter bodySmall End-aligned), and a full-width Button "Submit Review"/"Update Review" enabled when selectedRating>0 && !isSubmitting showing a 16dp onPrimary CircularProgressIndicator while submitting. Pre-fill all fields from state.existingReview when editing (D-05). On submit dispatch CustomerProductDetailAction.SubmitReview(rating,title,body); on success pop back (snackbar shown on detail). Add `@Serializable data class WriteReview(val productId: String, val existingReviewId: String? = null)` to AppNavigationObjects.kt and register the composable destination in AppNavigation.kt wiring it to the same CustomerProductDetailViewModel scope or a lightweight review VM that shares the repository (reuse the detail VM's submit path — do NOT duplicate business logic in the composable). Use exact copy strings and content descriptions from UI-SPEC.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:assembleDebug` compiles.
    - `grep -c "Verified Purchase" app/src/main/java/com/wenubey/wenucommerce/core/components/VerifiedPurchaseBadge.kt` returns 1.
    - `grep -c "Rate \$" app/src/main/java/com/wenubey/wenucommerce/customer/customer_reviews/StarRatingSelector.kt` >= 1 (accessible per-star contentDescription).
    - `grep -c "Summarise your experience\|What did you like or dislike" app/src/main/java/com/wenubey/wenucommerce/customer/customer_reviews/WriteReviewScreen.kt` >= 1 (UI-SPEC copy).
    - `grep -c "data class WriteReview" app/src/main/java/com/wenubey/wenucommerce/navigation/AppNavigationObjects.kt` returns 1.
    - `grep -c "0/1000\|/1000" app/src/main/java/com/wenubey/wenucommerce/customer/customer_reviews/WriteReviewScreen.kt` >= 1 (body counter).
  </acceptance_criteria>
  <done>Badge, selector, extracted+extended ReviewCard, WriteReviewScreen with pre-fill + counter + spinner, and the WriteReview route all exist and compile; copy + accessibility strings match UI-SPEC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire product detail screen — AggregateRatingHeader, ReviewSortToggle, gated affordance, badge cards, review states + Compose UI tests</name>
  <read_first>
    - app/.../customer/customer_products/CustomerProductDetailScreen.kt — existing reviews section item (analog: PATTERNS.md lines 459-468), the LazyColumn structure, the snackbar host + LaunchedEffect (lines 83-97), the auth-gate affordance pattern.
    - .planning/phases/07-reviews-ratings/07-UI-SPEC.md §C-04 (AggregateRatingHeader), §C-05 (ReviewSortToggle — SingleChoiceSegmentedButtonRow), §C-08 (WriteReviewAffordance gate rules), §"Screen States" (Loading/Empty/Populated/Error), §"Copywriting Contract".
    - .planning/phases/07-reviews-ratings/07-PATTERNS.md §"CustomerProductDetailScreen sort toggle + badge + write-review affordance".
    - .claude/skills/ Compose-testing skill — createComposeRule / createAndroidComposeRule, onNodeWithText/onNodeWithContentDescription, state-render + one-interaction assertions.
    - Any existing *ScreenTest.kt under app/src/androidTest for the project's Compose test harness style.
  </read_first>
  <behavior>
    CustomerProductDetailScreenTest (app/src/androidTest, Compose UI):
    - REVW-05: a review with isVerifiedPurchase=true renders a node with text "Verified Purchase"; a review with false does not.
    - D-07 gate: state.hasDeliveredOrder=false → no "Write a Review" / "Edit Your Review" node; hasDeliveredOrder=true && existingReview=null → "Write a Review" present; hasDeliveredOrder=true && existingReview!=null → "Edit Your Review" present.
    - REVW-04: with reviewCount>0 the aggregate header renders the formatted average and "N reviews"; with reviewCount==0 renders "No ratings yet" / empty-state copy "No reviews yet. Be the first to share your experience."
    - REVW-06: both sort segments "Most recent" and "Highest rated" render; the default selected is "Most recent".
  </behavior>
  <action>
    In CustomerProductDetailScreen.kt add, in the reviews region of the LazyColumn: (1) an AggregateRatingHeader item per C-04 — large numeric average `"%.1f".format(product.averageRating)` (headlineSmall bold) + StarRatingDisplay(floor(avg), Large 20dp) + "$reviewCount reviews" (bodyMedium onSurfaceVariant), or "No ratings yet" when reviewCount==0, with Row contentDescription per Accessibility; (2) a WriteReviewAffordance item per C-08 gated exactly: isCheckingEligibility → LinearProgressIndicator(2dp); hasDeliveredOrder && existingReview==null → TextButton "Write a Review" + RateReview icon (20dp, primary); hasDeliveredOrder && existingReview!=null → TextButton "Edit Your Review"; else render nothing — on tap navigate to WriteReview(productId, existingReviewId) (D-07/D-05); (3) a ReviewSortToggle item per C-05 using SingleChoiceSegmentedButtonRow over ReviewSortOrder.entries with labels "Most recent"/"Highest rated", emitting OnSortOrderChanged, per-segment contentDescription; (4) replace the inline ReviewCard usage with the extracted core/components/ReviewCard passing `hasVoted = review.id in state.helpfulVotedIds` and `onHelpful = { onAction(OnMarkReviewHelpful(review.id)) }`. Add the reviews Screen States per §"Screen States": loading (CircularProgressIndicator in reviews area), empty (copy exact), error (copy exact). Add a LaunchedEffect for state.reviewSubmitError to show the error snackbar (duration Long) then DismissReviewForm. Write CustomerProductDetailScreenTest per <behavior>.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug && ./gradlew :app:testDebugUnitTest --tests "*CustomerProductDetailViewModelTest"</automated>
    <human-check>Compose UI: ./gradlew :app:connectedDebugAndroidTest --tests "*CustomerProductDetailScreenTest" on an emulator (see UI-SPEC states). If no emulator in CI this session, the executor runs it on the next device-available run; assembleDebug + ViewModel tests gate the merge.</human-check>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:assembleDebug` compiles.
    - `grep -c "SingleChoiceSegmentedButtonRow" app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailScreen.kt` returns 1.
    - `grep -c "Most recent\|Highest rated" app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailScreen.kt` >= 2.
    - `grep -c "Write a Review\|Edit Your Review" app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailScreen.kt` >= 2 (gated affordance).
    - `grep -c "No reviews yet. Be the first" app/src/main/java/com/wenubey/wenucommerce/customer/customer_products/CustomerProductDetailScreen.kt` returns 1 (empty state copy).
    - CustomerProductDetailScreenTest.kt exists with Verified Purchase + gate + aggregate + sort assertions.
    - `./gradlew :app:testDebugUnitTest --tests "*CustomerProductDetailViewModelTest"` still green.
  </acceptance_criteria>
  <done>Product detail renders aggregate header, sort toggle, gated write/edit affordance, badge-enhanced cards, and all review states; Compose test authored; :app compiles and unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI eligibility gate → Cloud Function | The D-07 hasDeliveredOrder gate is a UX affordance ONLY; the submitReview Cloud Function (07-01) is the security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Spoofing | User bypasses the hidden UI affordance and calls submit directly | mitigate | Defense-in-depth: the UI gate hides the affordance (Task 1/3), but authority is the server submitReview function (07-01 T-07-01). No client-side trust. |
| T-07-09 | Tampering | Helpful double-tap from UI | mitigate | Optimistic disable via helpfulVotedIds (Task 1) + server already-exists guard (07-01 T-07-04) |
| T-07-10 | Input validation | Rating out of range / empty submit | mitigate | Submit button disabled while selectedRating==0 (Task 2); server re-validates rating 1-5 (07-01) |
| T-07-SC | Tampering | npm/gradle installs | accept | No new dependencies (RESEARCH: all libraries already in libs.versions.toml). No install task. |
</threat_model>

<verification>
- `./gradlew :app:testDebugUnitTest --tests "*CustomerProductDetailViewModelTest"` green (gate, pre-fill, sort tie-break, submit, helpful).
- `./gradlew :app:assembleDebug` green.
- CustomerProductDetailScreenTest authored (Verified Purchase badge, D-07 gate, aggregate header, sort segments); run on emulator when available.
- Copy + accessibility strings match 07-UI-SPEC.md exactly.
</verification>

<success_criteria>
- Write/Edit review affordance gated to DELIVERED-order customers (D-07); pre-filled on edit (D-05).
- Aggregate rating header shows average + count (REVW-04); Verified Purchase badge on verified reviews (REVW-05).
- Segmented sort toggle (Most recent default / Highest rated, recency tie-break) reorders the list (REVW-06).
- Submitting a 1-5 star review with optional text invokes submitReview and returns with feedback (REVW-01).
- Helpful action invokes markReviewHelpful with optimistic disable (D-04).
- ViewModel tests green; Compose UI test authored; UI-SPEC contract honored.
</success_criteria>

<output>
Create `.planning/phases/07-reviews-ratings/07-02-SUMMARY.md` when done.
</output>
