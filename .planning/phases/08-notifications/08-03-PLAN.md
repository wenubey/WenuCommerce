---
phase: 08-notifications
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - app/src/main/java/com/wenubey/wenucommerce/notification/OrderNotificationConstants.kt
  - app/src/main/java/com/wenubey/wenucommerce/notification/NotificationChannels.kt
  - app/src/main/java/com/wenubey/wenucommerce/notification/MessagingService.kt
  - app/src/main/java/com/wenubey/wenucommerce/notification/SyncEvent.kt
  - app/src/main/java/com/wenubey/wenucommerce/WenuCommerce.kt
  - app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt
  - app/src/main/java/com/wenubey/wenucommerce/di/DataModule.kt
  - data/src/main/java/com/wenubey/data/worker/FcmTokenWorker.kt
  - app/src/test/java/com/wenubey/wenucommerce/MessagingServiceSyncBusTest.kt
  - app/src/test/java/com/wenubey/wenucommerce/notification/NotificationChannelsTest.kt
  - data/src/test/java/com/wenubey/data/worker/FcmTokenWorkerTest.kt
autonomous: true
requirements: [NOTF-04, NOTF-06, NOTF-07]
must_haves:
  truths:
    - "Three notification channels (Order Updates HIGH, Account DEFAULT, Promotions LOW) are created once in Application.onCreate"
    - "MessagingService routes order_status / new_order / new_review / device_login to the correct channel and deep-link"
    - "Tapping a new_review notification navigates to SellerProductReviews(productId, productTitle)"
    - "onNewToken enqueues a unique FcmTokenWorker (not fire-and-forget) that awaits updateFcmToken"
    - "FcmTokenWorker retries on failure up to MAX_RETRIES and uses a work name distinct from SyncWorker"
  artifacts:
    - path: "app/src/main/java/com/wenubey/wenucommerce/notification/NotificationChannels.kt"
      provides: "centralised createAll() for the three channels"
      contains: "createNotificationChannels"
    - path: "data/src/main/java/com/wenubey/data/worker/FcmTokenWorker.kt"
      provides: "one-time WorkManager token-refresh job"
      contains: "UNIQUE_WORK_NAME = \"fcm_token_refresh\""
    - path: "app/src/main/java/com/wenubey/wenucommerce/notification/MessagingService.kt"
      provides: "unified type router + showNewReviewNotification + buildNewReviewNotificationIntent + onNewToken enqueue"
      contains: "FCM_TYPE_NEW_REVIEW"
  key_links:
    - from: "app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt"
      to: "SellerProductReviews route"
      via: "new_review deep-link branch in the LaunchedEffect when block"
      pattern: "SellerProductReviews\\("
    - from: "app/src/main/java/com/wenubey/wenucommerce/notification/MessagingService.kt onNewToken"
      to: "FcmTokenWorker.enqueue"
      via: "onNewToken body"
      pattern: "FcmTokenWorker\\.enqueue"
    - from: "app/src/main/java/com/wenubey/wenucommerce/WenuCommerce.kt onCreate"
      to: "NotificationChannels.createAll"
      via: "onCreate before startKoin"
      pattern: "NotificationChannels\\.createAll"
---

<objective>
Wire the Android delivery side: centralise the three notification channels (NOTF-06),
extend MessagingService into a unified type router that handles the new `new_review` type
(NOTF-04) plus channel migration (D-03/D-06), add the `new_review` deep-link branch to
MainActivity so a review tap opens `SellerProductReviews` (NOTF-04), and replace the
fire-and-forget `onNewToken` with a unique WorkManager `FcmTokenWorker` (NOTF-07).

Purpose: 08-02 now sends `new_review` FCM on `order_updates_channel`; the client must declare
that channel and route the type. 08-01 made `updateFcmToken` suspend so `FcmTokenWorker` can
`await()` the write. This plan connects both server changes to the device.

Output: NotificationChannels helper + WenuCommerce.onCreate call; extended OrderNotificationConstants
(new_review + 3 channel ids); MessagingService router + showNewReviewNotification +
buildNewReviewNotificationIntent + onNewToken→enqueue + channelId swap; MainActivity new_review
branch + clearNotificationExtras; FcmTokenWorker + Koin registration; unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-notifications/08-CONTEXT.md
@.planning/phases/08-notifications/08-RESEARCH.md
@.planning/phases/08-notifications/08-PATTERNS.md
@.planning/phases/08-notifications/08-VALIDATION.md

<read_first>
Analogs to clone (read before writing — do NOT re-read after):
- app/.../notification/OrderNotificationConstants.kt (full, lines 1–33) — flat const val pattern (PATTERNS §OrderNotificationConstants)
- app/.../notification/MessagingService.kt (full) — onMessageReceived router (48–75), showNewOrderNotification (112–139), buildNewOrderNotificationIntent (246–263), ensureOrderStatusChannel (197–209), onNewToken (43–46), showNotification device_login (143–156)
- app/.../WenuCommerce.kt (lines 14–35) — onCreate, insertion point before startKoin (PATTERNS §NotificationChannels — WenuCommerce insertion)
- app/.../MainActivity.kt (lines 113–147, clearNotificationExtras 232–237) — deep-link LaunchedEffect + dual raw/namespaced extra read (PATTERNS §MainActivity)
- data/.../worker/SyncWorker.kt (full) — CoroutineWorker + doWork + companion enqueue with CONNECTED + EXPONENTIAL + REPLACE (PATTERNS §FcmTokenWorker)
- app/.../di/DataModule.kt (workerModule line 173, single FirebaseMessaging line 69) — worker{} registration + FirebaseMessaging single
- app/src/test/java/com/wenubey/wenucommerce/MessagingServiceSyncBusTest.kt — existing intent-builder + emitSyncIf* test to extend with new_review
- app/.../navigation/AppNavigationObjects.kt line 57 — SellerProductReviews(val productId, val productTitle) route (deep-link target; no change)
</read_first>

<interfaces>
OrderNotificationConstants additions (PATTERNS §OrderNotificationConstants):
  FCM_TYPE_NEW_REVIEW = "new_review"
  FCM_DATA_KEY_PRODUCT_ID = "productId"; FCM_DATA_KEY_PRODUCT_TITLE = "productTitle"
  FCM_DATA_KEY_NOTIF_ID = "notifId"          // matches server data.notifId from 08-02
  NAV_TARGET_NEW_REVIEW = "new_review"
  EXTRA_PRODUCT_ID = "wenucommerce.product_id"; EXTRA_PRODUCT_TITLE = "wenucommerce.product_title"
  ORDER_UPDATES_CHANNEL_ID = "order_updates_channel"   // MUST equal server channelId from 08-02
  ACCOUNT_CHANNEL_ID = "account_channel"; PROMOTIONS_CHANNEL_ID = "promotions_channel"
  (keep ORDER_STATUS_CHANNEL_ID for backward compat / existing tests)

NotificationChannels (PATTERNS §NotificationChannels / RESEARCH §10):
  object NotificationChannels { fun createAll(context: Context) }  — creates 3 channels via nm.createNotificationChannels(listOf(...)); no-op below API 26

FcmTokenWorker constructor (PATTERNS §FcmTokenWorker / RESEARCH §14):
  FcmTokenWorker(appContext, params, firestoreRepository: FirestoreRepository, firebaseMessaging: FirebaseMessaging, dispatcherProvider: DispatcherProvider)
  companion: UNIQUE_WORK_NAME = "fcm_token_refresh"; fun enqueue(context)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralise 3 channels + extend OrderNotificationConstants (NOTF-06)</name>
  <files>app/src/main/java/com/wenubey/wenucommerce/notification/OrderNotificationConstants.kt, app/src/main/java/com/wenubey/wenucommerce/notification/NotificationChannels.kt, app/src/main/java/com/wenubey/wenucommerce/WenuCommerce.kt, app/src/main/java/com/wenubey/wenucommerce/notification/MessagingService.kt, app/src/test/java/com/wenubey/wenucommerce/notification/NotificationChannelsTest.kt</files>
  <behavior>
    - NotificationChannelsTest (Robolectric): after NotificationChannels.createAll(context), NotificationManager reports exactly three channels with ids order_updates_channel / account_channel / promotions_channel and importances HIGH / DEFAULT / LOW respectively.
    - Below API 26 (or when guarded), createAll is a no-op and does not throw.
  </behavior>
  <action>
Add the new constants from the <interfaces> block to OrderNotificationConstants.kt (keep ORDER_STATUS_CHANNEL_ID). Create NotificationChannels.kt as `object NotificationChannels` with `fun createAll(context: Context)`: early-return if Build.VERSION.SDK_INT < O; get NotificationManager; call createNotificationChannels(listOf(...)) with Order Updates (ORDER_UPDATES_CHANNEL_ID, "Order Updates", IMPORTANCE_HIGH, description "Order status changes, new orders, and new product reviews"), Account (ACCOUNT_CHANNEL_ID, "Account", IMPORTANCE_DEFAULT, "Device login and security alerts"), Promotions (PROMOTIONS_CHANNEL_ID, "Promotions", IMPORTANCE_LOW, "Promotional offers and discounts") — clone ensureOrderStatusChannel (MessagingService lines 197–209). In WenuCommerce.onCreate, add `NotificationChannels.createAll(this)` before the startKoin call (after FirebaseApp/PaymentConfiguration init). Remove the ensureOrderStatusChannel() call from MessagingService.onCreate (line 34) and remove the inline device_login_channel creation from showNotification (lines 143–156) — channels now come solely from NotificationChannels. Write NotificationChannelsTest.kt (Robolectric, mirror the project's existing Robolectric test config) asserting the three channels + importances per the behavior block.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*NotificationChannels*"</automated>
  </verify>
  <done>Three channels created centrally with correct ids/importances; MessagingService no longer creates channels; NotificationChannelsTest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MessagingService new_review router + intent builder + onNewToken enqueue + channel swap (NOTF-04, NOTF-07)</name>
  <files>app/src/main/java/com/wenubey/wenucommerce/notification/MessagingService.kt, app/src/main/java/com/wenubey/wenucommerce/notification/SyncEvent.kt, app/src/main/java/com/wenubey/wenucommerce/MainActivity.kt, data/src/main/java/com/wenubey/data/worker/FcmTokenWorker.kt, app/src/main/java/com/wenubey/wenucommerce/di/DataModule.kt, app/src/test/java/com/wenubey/wenucommerce/MessagingServiceSyncBusTest.kt, data/src/test/java/com/wenubey/data/worker/FcmTokenWorkerTest.kt</files>
  <behavior>
    - buildNewReviewNotificationIntent(context, data) returns null when data[type] != "new_review" or productId is blank; otherwise returns the launch intent with EXTRA_NAV_TARGET=NAV_TARGET_NEW_REVIEW, EXTRA_PRODUCT_ID=productId, EXTRA_PRODUCT_TITLE=productTitle, and CLEAR_TOP|SINGLE_TOP flags.
    - emitSyncIfNewReview(syncBus, data) returns false for non-new_review or blank productId, true after emitting SyncEvent.NewReview(productId) otherwise.
    - FcmTokenWorkerTest: doWork returns Result.success when firebaseMessaging.token resolves and updateFcmToken succeeds; returns Result.retry (below MAX_RETRIES) when updateFcmToken fails; UNIQUE_WORK_NAME == "fcm_token_refresh" (distinct from SyncWorker's).
  </behavior>
  <action>
Per D-04 (route new_review + deep-link to SellerProductReviews) and D-05 (onNewToken → WorkManager). In MessagingService.onMessageReceived, add a `new_review` branch before the legacy device_login fallback (clone lines 48–75): when data[FCM_DATA_KEY_TYPE] == FCM_TYPE_NEW_REVIEW → showNewReviewNotification(message); return. Add showNewReviewNotification cloning showNewOrderNotification (112–139) but reading FCM_DATA_KEY_PRODUCT_ID, using buildNewReviewNotificationIntent, and NotificationCompat.Builder(this, ORDER_UPDATES_CHANNEL_ID). Add internal buildNewReviewNotificationIntent(context, data): Intent? cloning buildNewOrderNotificationIntent (246–263) — return null if type != FCM_TYPE_NEW_REVIEW or productId blank; putExtra(EXTRA_NAV_TARGET, NAV_TARGET_NEW_REVIEW), putExtra(EXTRA_PRODUCT_ID, productId), putExtra(EXTRA_PRODUCT_TITLE, productTitle). Update showOrderStatusNotification and showNewOrderNotification NotificationCompat.Builder channel arg from ORDER_STATUS_CHANNEL_ID → ORDER_UPDATES_CHANNEL_ID. Add SyncEvent.NewReview(val productId: String) to SyncEvent.kt and an internal emitSyncIfNewReview companion helper (RESEARCH §Code Examples), wiring it in the new_review branch via serviceScope.launch (optional refresh; keep consistent with emitSyncIfNewOrder). Replace onNewToken body (43–46) with `super.onNewToken(token); FcmTokenWorker.enqueue(this)`.
Create FcmTokenWorker.kt in :data cloning SyncWorker (PATTERNS §FcmTokenWorker): constructor per <interfaces>; doWork() = dispatcherProvider.io().run { try { val token = firebaseMessaging.token.await(); firestoreRepository.updateFcmToken(token).getOrThrow(); Result.success() } catch (e) { Timber.e; if (runAttemptCount < MAX_RETRIES) Result.retry() else Result.failure() } }; companion MAX_RETRIES=3, UNIQUE_WORK_NAME="fcm_token_refresh", enqueue(context) building OneTimeWorkRequest with CONNECTED + EXPONENTIAL(30s) + enqueueUniqueWork(REPLACE). Register in DataModule workerModule: `worker { FcmTokenWorker(get(), get(), get(), get(), get()) }` (FirebaseMessaging single already at line 69).
In MainActivity, extend the deep-link LaunchedEffect (PATTERNS §MainActivity): read namespacedProductId/fcmRawProductId → productId, productTitle (namespaced ?: raw ?: ""), isNewReview = target == NAV_TARGET_NEW_REVIEW || fcmRawType == FCM_TYPE_NEW_REVIEW; add as the first when branch: `isNewReview && !productId.isNullOrBlank() -> { navController.navigate(SellerProductReviews(productId, productTitle)); clearNotificationExtras() }`. Extend clearNotificationExtras() to removeExtra EXTRA_PRODUCT_ID, EXTRA_PRODUCT_TITLE, "productId", "productTitle".
Extend MessagingServiceSyncBusTest.kt with new_review cases: buildNewReviewNotificationIntent returns correct extras + null on blank/mismatch; emitSyncIfNewReview true/false paths. Create FcmTokenWorkerTest.kt in :data using TestListenableWorkerBuilder (mock FirebaseMessaging.token via a completed Task, mock FirestoreRepository) asserting success/retry per the behavior block and the distinct UNIQUE_WORK_NAME.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*MessagingService*" && ./gradlew :data:testDebugUnitTest --tests "*FcmTokenWorker*"</automated>
  </verify>
  <done>new_review routes to a notification on order_updates_channel; buildNewReviewNotificationIntent extras correct; MainActivity navigates to SellerProductReviews on new_review tap; onNewToken enqueues FcmTokenWorker; worker succeeds/retries correctly with a distinct work name; both test targets green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FCM data payload → MessagingService/MainActivity | Untrusted FCM data extras drive channel + deep-link routing |
| WorkManager token job → Firestore USERS doc | Trusted write of the device FCM token for the signed-in user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | crafted FCM new_review with blank/absent productId | mitigate | buildNewReviewNotificationIntent + MainActivity branch require non-blank productId; return null / no-nav otherwise (Task 2) |
| T-08-10 | Spoofing | FCM-token hijack via stale/dropped token update | mitigate | onNewToken → WorkManager unique job with retry survives process death; awaits updateFcmToken result (Task 2, NOTF-07) |
| T-08-11 | DoS | FcmTokenWorker collides with SyncWorker unique name | mitigate | UNIQUE_WORK_NAME = "fcm_token_refresh" distinct from "sync_pending_operations" (Task 2, RESEARCH §Pitfall 7) |
| T-08-SC | Tampering | gradle installs | accept | no new packages; all libs already in libs.versions.toml (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `./gradlew :app:testDebugUnitTest --tests "*MessagingService*"` + `--tests "*NotificationChannels*"` green
- `./gradlew :data:testDebugUnitTest --tests "*FcmTokenWorker*"` green
- `./gradlew :app:assembleDebug` green (smoke)
- On-device deep-link tap + system dialog remain MANUAL verifications (08-VALIDATION Manual-Only table)
</verification>

<success_criteria>
- Three channels exist in system settings (NOTF-06)
- new_review notification routes + deep-links to SellerProductReviews (NOTF-04)
- Token refresh via unique WorkManager job, not fire-and-forget (NOTF-07)
- All notification sends use order_updates_channel, matching 08-02's server channelId
- Automated test targets + assembleDebug green
</success_criteria>

<output>
Create `.planning/phases/08-notifications/08-03-SUMMARY.md` when done
</output>
