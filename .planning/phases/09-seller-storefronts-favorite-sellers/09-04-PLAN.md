---
phase: 09-seller-storefronts-favorite-sellers
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-03"]
files_modified:
  - app/src/main/java/com/wenubey/wenucommerce/customer/customer_followed_sellers/FollowedSellersState.kt
  - app/src/main/java/com/wenubey/wenucommerce/customer/customer_followed_sellers/FollowedSellersAction.kt
  - app/src/main/java/com/wenubey/wenucommerce/customer/customer_followed_sellers/FollowedSellersViewModel.kt
  - app/src/main/java/com/wenubey/wenucommerce/customer/customer_followed_sellers/FollowedSellersScreen.kt
  - app/src/main/java/com/wenubey/wenucommerce/customer/CustomerProfileScreen.kt
  - app/src/main/java/com/wenubey/wenucommerce/seller/seller_dashboard/SellerDashboardState.kt
  - app/src/main/java/com/wenubey/wenucommerce/seller/seller_dashboard/SellerDashboardViewModel.kt
  - app/src/main/java/com/wenubey/wenucommerce/seller/seller_dashboard/SellerDashboardScreen.kt
  - app/src/main/java/com/wenubey/wenucommerce/seller/SellerProfileScreen.kt
  - app/src/main/java/com/wenubey/wenucommerce/navigation/AppNavigationObjects.kt
  - app/src/main/java/com/wenubey/wenucommerce/navigation/TabNavRoutes.kt
  - app/src/main/java/com/wenubey/wenucommerce/di/ViewmodelModule.kt
  - app/src/test/java/com/wenubey/wenucommerce/customer/customer_followed_sellers/FollowedSellersViewModelTest.kt
  - app/src/test/java/com/wenubey/wenucommerce/seller/seller_dashboard/SellerDashboardViewModelTest.kt
  - app/src/androidTest/java/com/wenubey/wenucommerce/customer/customer_followed_sellers/FollowedSellersScreenTest.kt
autonomous: true
requirements: [FAVS-02, FAVS-04]
user_setup: []

must_haves:
  truths:
    - "A customer opens Followed Sellers from a Profile row and sees the list of sellers they follow, loaded from Room (fast offline)"
    - "Tapping Unfollow on a row removes it immediately; a Firestore failure re-adds the row and shows a snackbar"
    - "Tapping a followed-seller row navigates to that seller's storefront"
    - "A seller sees their follower COUNT on the dashboard stat card and on the profile quick-stats — never a list of follower identities"
    - "Seller dashboard/profile show the live aggregate rating (or an em dash at 0 reviews), replacing the hardcoded 4.8 placeholder"
  artifacts:
    - path: "app/src/main/java/com/wenubey/wenucommerce/customer/customer_followed_sellers/FollowedSellersViewModel.kt"
      provides: "Observes FollowedSellersRepository Room Flow; optimistic unfollow"
      contains: "observeFollowedSellers"
    - path: "app/src/main/java/com/wenubey/wenucommerce/customer/customer_followed_sellers/FollowedSellersScreen.kt"
      provides: "Scaffold + LazyColumn of FollowedSellerRow + empty state + pull-to-refresh"
      contains: "FollowedSellerRow"
    - path: "app/src/main/java/com/wenubey/wenucommerce/navigation/AppNavigationObjects.kt"
      provides: "FollowedSellers @Serializable data object route"
      contains: "data object FollowedSellers"
    - path: "app/src/main/java/com/wenubey/wenucommerce/seller/seller_dashboard/SellerDashboardScreen.kt"
      provides: "Followers stat card (M3 token) + live Rating card replacing hardcoded 4.8"
      contains: "Followers"
  key_links:
    - from: "app/.../customer_followed_sellers/FollowedSellersViewModel.kt"
      to: "FollowedSellersRepository"
      via: "observeFollowedSellers(userId) Room Flow + unfollowSeller"
      pattern: "followedSellersRepository\\.(observeFollowedSellers|unfollowSeller)"
    - from: "app/.../customer/CustomerProfileScreen.kt"
      to: "FollowedSellers route"
      via: "ProfileMenuItem onClick = onNavigateToFollowedSellers"
      pattern: "onNavigateToFollowedSellers"
    - from: "app/.../navigation/TabNavRoutes.kt"
      to: "FollowedSellersScreen"
      via: "composable<FollowedSellers> { FollowedSellersScreen(...) }"
      pattern: "composable<FollowedSellers>"
    - from: "app/.../seller/seller_dashboard/SellerDashboardViewModel.kt"
      to: "User.followerCount"
      via: "firestoreRepository.getUser(currentUserId) → state.followerCount"
      pattern: "followerCount"
---

<objective>
Deliver the Followed Sellers list and the seller-side follower-count surfaces. Create `FollowedSellersScreen/State/Action/ViewModel` (clone of the wishlist list-screen pattern, minus multi-select/undo/cart) that observes the Room follow Flow with optimistic unfollow; add a "Followed Sellers" `ProfileMenuItem` row in `CustomerProfileScreen` and the standalone `FollowedSellers` route (D-04, mirrors NotificationHistory); and replace the hardcoded seller-dashboard/profile placeholders with the live follower count (FAVS-04, count only) and live aggregate rating.

Purpose: Delivers FAVS-02 (followed-sellers list) and FAVS-04 (seller sees follower count, not identities).

Output: FollowedSellers feature (state/action/VM/screen); Profile row + route wiring; SellerDashboard + SellerProfile follower-count/rating surfaces; ViewModel + Compose UI tests.

DEPENDS ON 09-01 (FollowedSellersRepository, User.followerCount) AND 09-03 (this plan also edits `AppNavigationObjects.kt` + `TabNavRoutes.kt`, which 09-03 modifies — sequential to avoid a merge conflict; it runs in Wave 3 after 09-03's Wave 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-seller-storefronts-favorite-sellers/09-CONTEXT.md
@.planning/phases/09-seller-storefronts-favorite-sellers/09-RESEARCH.md
@.planning/phases/09-seller-storefronts-favorite-sellers/09-PATTERNS.md
@.planning/phases/09-seller-storefronts-favorite-sellers/09-UI-SPEC.md
@.planning/phases/09-seller-storefronts-favorite-sellers/09-VALIDATION.md

<interfaces>
<!-- From 09-01 (merged): -->
FollowedSellersRepository.observeFollowedSellers(userId): Flow<List<FollowedSeller>>
FollowedSellersRepository.unfollowSeller(userId, sellerId)
FollowedSeller(sellerId, sellerName, sellerLogoUrl, followedAt)
User.followerCount: Int   // via FirestoreRepositoryImpl.getUser()

<!-- From 09-03 (merged): reuse the same route-nav idiom -->
SellerStorefront(sellerId)  // navigate here on a row tap

<!-- Existing to reuse: -->
ProfileMenuItem(icon, title, subtitle, onClick)   // CustomerProfileScreen.kt lines 142-188
SellerStatsCard(title, value, icon, color)        // SellerDashboardScreen.kt lines 296-330
StatItem(label, value)                            // SellerProfileScreen.kt lines 222-239
firestoreRepository.getUser(uid): Result<User>    // used by SellerDashboardViewModel to read followerCount
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FollowedSellers State/Action/ViewModel + SellerDashboard follower-count/rating state, DI registration, ViewModel tests</name>
  <read_first>
    - app/src/main/java/com/wenubey/wenucommerce/customer/customer_wishlist/WishlistState.kt (analog; 09-PATTERNS.md lines 403-428)
    - app/src/main/java/com/wenubey/wenucommerce/customer/customer_wishlist/WishlistAction.kt (analog; 09-PATTERNS.md lines 432-455)
    - app/src/main/java/com/wenubey/wenucommerce/customer/customer_wishlist/WishlistViewModel.kt (analog — observe + optimistic remove; 09-PATTERNS.md lines 459-519)
    - app/src/main/java/com/wenubey/wenucommerce/seller/seller_dashboard/SellerDashboardViewModel.kt (target — observeCurrentUser; add getUser followerCount + rating)
    - app/src/main/java/com/wenubey/wenucommerce/seller/seller_dashboard/SellerDashboardState.kt (target — add followerCount/averageRating/reviewCount; 09-PATTERNS.md line 947)
    - app/src/main/java/com/wenubey/wenucommerce/di/ViewmodelModule.kt (target — WishlistViewModel registration line 65; 09-PATTERNS.md lines 1078-1090)
    - 09-CONTEXT.md CD-01 (list clone minus anon), D-04 (list from Profile), FAVS-04 (count only)
    - 09-VALIDATION.md rows FAVS-02 (VM emits list) + FAVS-04 (dashboard count-only)
    - .claude/skills — Turbine, coroutines/runTest, MockK
  </read_first>
  <behavior>
    FollowedSellersViewModelTest (new; Turbine + MainDispatcherRule + FakeFollowedSellersRepository + FakeAuthRepository):
    - emitsListFromRoom: repo Flow emits [FollowedSeller A, B] → state.sellers == [A, B], isLoading false.
    - unfollow_removesRowOptimistically: onAction OnUnfollow(A.sellerId) → A removed from state.sellers immediately.
    - unfollow_revertsOnFailure: fake repo unfollowSeller throws → A re-appears in state.sellers, errorMessage set.
    - emptyWhenNoFollows: repo emits [] → state.sellers empty, isLoading false.
    SellerDashboardViewModelTest (new or extend; FakeFirestoreRepository returning User(followerCount = N, and products with ratings)):
    - surfacesFollowerCountOnly: getUser returns User(followerCount = 12) → state.followerCount == 12; assert NO API/field exposes a follower identity list (state has no follower-identity collection — count only, FAVS-04).
    - surfacesAggregateRating: given the seller's active products, state.averageRating == weighted mean and state.reviewCount == total (or 0 → rating renders em dash downstream).
  </behavior>
  <action>
    Create `customer/customer_followed_sellers/FollowedSellersState.kt`: `data class FollowedSellersState(sellers: List<FollowedSeller> = emptyList(), isLoading: Boolean = true, isRefreshing: Boolean = false, errorMessage: String? = null)`.
    Create `customer/customer_followed_sellers/FollowedSellersAction.kt`: `sealed interface FollowedSellersAction { data class OnUnfollow(val sellerId: String); data object OnRefresh; data class NavigateToStorefront(val sellerId: String) }`.
    Create `customer/customer_followed_sellers/FollowedSellersViewModel.kt`: constructor `(followedSellersRepository, authRepository, dispatcherProvider)`. In init read `userId = authRepository.currentUser.value?.uuid ?: ""` then `observeFollowedSellers(userId)` (collect → state.sellers, isLoading=false; .catch → errorMessage). `unfollow(sellerId)`: optimistic remove from state.sellers, launch io runCatching `unfollowSeller(userId, sellerId)`, onFailure re-add + errorMessage (mirrors WishlistViewModel remove; NO undo/multi-select/cart). `onAction` dispatches OnUnfollow/OnRefresh/NavigateToStorefront.
    Modify `SellerDashboardState.kt`: add `followerCount: Int = 0`, `averageRating: Double = 0.0`, `reviewCount: Int = 0`.
    Modify `SellerDashboardViewModel.kt`: in the current-user load path, call `firestoreRepository.getUser(currentUserId)` and set `state.followerCount = user.followerCount` (FAVS-04 count only — do NOT fetch or expose any follower identity list). Compute `averageRating`/`reviewCount` as the weighted mean over the seller's active products (reuse the same CD-04 formula as 09-03; extract a shared helper or inline).
    Modify `di/ViewmodelModule.kt`: add `viewModelOf(::FollowedSellersViewModel)` after WishlistViewModel. (SellerDashboardViewModel already registered — Koin injects the new getUser usage; no signature change to its constructor.)
    Create `FollowedSellersViewModelTest.kt` + create/extend `SellerDashboardViewModelTest.kt` per the behavior block.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*FollowedSellersViewModelTest" --tests "*SellerDashboardViewModelTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "sealed interface FollowedSellersAction" app/.../customer_followed_sellers/FollowedSellersAction.kt` with OnUnfollow/OnRefresh/NavigateToStorefront and NO multi-select/undo/cart actions (behavior: simplified vs wishlist)
    - `grep -q "observeFollowedSellers" app/.../customer_followed_sellers/FollowedSellersViewModel.kt` and `grep -q "unfollowSeller" ...FollowedSellersViewModel.kt` (key_link: Room-backed list + optimistic unfollow)
    - `grep -q "followerCount" app/.../seller/seller_dashboard/SellerDashboardViewModel.kt` reading from getUser (key_link: FAVS-04 count) and the file exposes NO follower-identity collection (grep for a follower-list field returns nothing)
    - `grep -q "viewModelOf(::FollowedSellersViewModel)" app/src/main/java/com/wenubey/wenucommerce/di/ViewmodelModule.kt` (source: DI registration)
    - `./gradlew :app:testDebugUnitTest --tests "*FollowedSellersViewModelTest" --tests "*SellerDashboardViewModelTest"` GREEN (list-from-Room, optimistic unfollow + revert, follower-count-only surface)
  </acceptance_criteria>
  <done>FollowedSellers State/Action/ViewModel exist (Room-backed list, optimistic unfollow, no undo/multi-select/cart); SellerDashboard surfaces followerCount (count only) + live rating; FollowedSellersViewModel DI-registered; ViewModel tests green.</done>
</task>

<task type="auto">
  <name>Task 2: FollowedSellersScreen + FollowedSellers route + Profile row + SellerDashboard/SellerProfile surfaces + Compose test</name>
  <read_first>
    - app/src/main/java/com/wenubey/wenucommerce/customer/CustomerWishlistScreen.kt (analog — Scaffold/TopAppBar/empty state; 09-PATTERNS.md lines 522-583)
    - app/src/main/java/com/wenubey/wenucommerce/customer/CustomerProfileScreen.kt (target — ProfileMenuItem lines 142-188, Wishlist row lines 96-103; 09-PATTERNS.md lines 850-900)
    - app/src/main/java/com/wenubey/wenucommerce/navigation/AppNavigationObjects.kt (target — NotificationHistory data object line 134; 09-PATTERNS.md lines 983-997)
    - app/src/main/java/com/wenubey/wenucommerce/navigation/TabNavRoutes.kt (target — composable<NotificationHistory> lines 50-63, composable<CustomerTab>; 09-PATTERNS.md lines 1001-1041)
    - app/src/main/java/com/wenubey/wenucommerce/seller/seller_dashboard/SellerDashboardScreen.kt (target — SellerStatsCard lines 296-330, hardcoded Rating "4.8" lines 128-135; 09-PATTERNS.md lines 904-949)
    - app/src/main/java/com/wenubey/wenucommerce/seller/SellerProfileScreen.kt (target — StatItem lines 222-239, hardcoded stats row lines 157-163, "4.8 (127 reviews)" lines 116-119; 09-PATTERNS.md lines 952-980)
    - 09-UI-SPEC.md §2 (FollowedSellersScreen + FollowedSellerRow + empty state), §3 (Profile row placement Orders→Wishlist→Followed Sellers→Addresses), §4a (dashboard Followers card + live Rating), §4b (profile Followers + Rating), Copywriting + Accessibility
    - 09-CONTEXT.md D-04 (route + Profile row, no new tab), CD-04 (rating), Pitfall 6 (no hardcoded color on the new Followers card)
  </read_first>
  <action>
    Create `customer/customer_followed_sellers/FollowedSellersScreen.kt`: params `onNavigateBack: () -> Unit`, `onNavigateToStorefront: (String) -> Unit`, `viewModel = koinViewModel()`. `Scaffold` + `TopAppBar(title="Followed Sellers", navigationIcon=ArrowBack→onNavigateBack)` + `SnackbarHost`. State machine per 09-UI-SPEC §2 (Loading centered spinner; Empty → FollowedSellersEmptyState; Populated → LazyColumn of FollowedSellerRow keyed by sellerId, 8dp spacing, 16dp contentPadding). Private `FollowedSellerRow`: Card(clickable → onNavigateToStorefront(sellerId)) with Row[AsyncImage 48dp CircleShape (Store fallback) + Column(weight1)(sellerName titleSmall SemiBold ellipsis; "Followed" bodySmall) + OutlinedButton("Unfollow", colorScheme.error content + border → onAction OnUnfollow) + Icons.Default.ChevronRight]. Private `FollowedSellersEmptyState`: Icons.Outlined.FavoriteBorder 80dp alpha 0.5 + "No followed sellers yet" + "Tap a seller's name on any product to visit their storefront and follow them." (Copywriting Contract; NO CTA button). Wrap list in `PullToRefreshBox`. Optimistic unfollow with snackbar "Couldn't unfollow. Try again." on failure; NO undo snackbar (per UI-SPEC §2).
    Modify `AppNavigationObjects.kt`: add `@Serializable data object FollowedSellers` after NotificationHistory (mirrors line 134).
    Modify `TabNavRoutes.kt`: add `composable<FollowedSellers> { FollowedSellersScreen(onNavigateBack = { navController.navigateUp() }, onNavigateToStorefront = { sellerId -> navController.navigate(SellerStorefront(sellerId)) }) }`. Thread `onNavigateToFollowedSellers = { navController.navigate(FollowedSellers) }` into `composable<CustomerTab>` → CustomerTabScreen → CustomerProfileScreen.
    Modify `CustomerProfileScreen.kt`: add `onNavigateToFollowedSellers: () -> Unit = {}` param; insert a `ProfileMenuItem(icon = Icons.Default.Favorite, title = "Followed Sellers", subtitle = "Sellers you're following", onClick = onNavigateToFollowedSellers)` immediately AFTER the Wishlist row and BEFORE Addresses (09-UI-SPEC §3 ordering).
    Modify `SellerDashboardScreen.kt`: add a `SellerStatsCard(title = "Followers", value = formatFollowerCount(state.followerCount), icon = Icons.Default.Group, color = MaterialTheme.colorScheme.primary)` — M3 token, NOT hardcoded (Pitfall 6). Replace the hardcoded "4.8" Rating card value with `if (state.reviewCount > 0) "%.1f".format(state.averageRating) else "–"` (keep the existing amber `Color(0xFFFF9800)` star color — established convention). Reuse/import `formatFollowerCount` (extract it to a shared location, e.g. core/util, so both the storefront and dashboard use one implementation — or duplicate the small pure function; either is acceptable, prefer shared).
    Modify `SellerProfileScreen.kt`: replace `StatItem("Customers", "89")` → `StatItem("Followers", formatFollowerCount(state.followerCount))`; replace `StatItem("Rating", "4.8")` and the "4.8 (127 reviews)" shop-info text → live `if (reviewCount>0) "%.1f".format(averageRating) else "–"`. Pass the seller's follower/rating state in (mirror how `user: User?` is currently threaded). Keep Total Sales / Orders / Products unchanged (out of scope).
    Create `FollowedSellersScreenTest.kt` (Compose UI, mockk + MutableStateFlow like SellerStorefrontScreenTest): renders_list_of_rows, renders_empty_state_when_no_sellers, renders_unfollow_button_per_row. Device-gated per project convention.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "data object FollowedSellers" app/src/main/java/com/wenubey/wenucommerce/navigation/AppNavigationObjects.kt` (source: route)
    - `grep -q "composable<FollowedSellers>" app/src/main/java/com/wenubey/wenucommerce/navigation/TabNavRoutes.kt` (key_link: route → screen)
    - `grep -q "onNavigateToFollowedSellers" app/src/main/java/com/wenubey/wenucommerce/customer/CustomerProfileScreen.kt` and `grep -q "Followed Sellers" ...CustomerProfileScreen.kt` (key_link: D-04 Profile row)
    - `grep -q "FollowedSellerRow" app/.../customer_followed_sellers/FollowedSellersScreen.kt` and `grep -q "FavoriteBorder" ...FollowedSellersScreen.kt` (source: row + empty state)
    - `grep -q "Followers" app/src/main/java/com/wenubey/wenucommerce/seller/seller_dashboard/SellerDashboardScreen.kt` and the new Followers card uses `colorScheme.primary` (no `Color(0xFF` on the Followers card — Pitfall 6)
    - Hardcoded rating removed: `grep -c '"4.8"' app/src/main/java/com/wenubey/wenucommerce/seller/seller_dashboard/SellerDashboardScreen.kt` returns 0 AND `grep -c '"4.8"' app/src/main/java/com/wenubey/wenucommerce/seller/SellerProfileScreen.kt` returns 0 (behavior: live rating replaces placeholder)
    - `grep -q "Followers" app/src/main/java/com/wenubey/wenucommerce/seller/SellerProfileScreen.kt` and the hardcoded `"89"` Customers stat is gone: `grep -c '"89"' ...SellerProfileScreen.kt` returns 0 (behavior: FAVS-04 surface)
    - `./gradlew :app:assembleDebug` GREEN
    - Device-gated (deferred): `./gradlew :app:connectedDebugAndroidTest --tests "*FollowedSellersScreenTest"` proves list/empty/unfollow rendering.
  </acceptance_criteria>
  <done>FollowedSellersScreen (list/empty/unfollow/pull-to-refresh) exists; FollowedSellers route + Profile row wired (D-04); SellerDashboard has a Followers stat card (M3 token) + live rating; SellerProfile shows Followers + live rating; hardcoded 4.8/89 placeholders removed; Compose UI test authored; :app:assembleDebug green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Customer → own follow list | Reads/mutates only their own Room + Firestore follow docs |
| Seller → follower data | Must see ONLY the aggregate count, never a follower identity list (FAVS-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Info Disclosure | Seller dashboard/profile follower surface | mitigate | SellerDashboardViewModel reads ONLY the scalar `User.followerCount`; the state carries no follower-identity collection and no query enumerates followers. Verified by the surfacesFollowerCountOnly test asserting the count is present and no identity list exists (FAVS-04). |
| T-09-14 | Tampering | Unfollow drift from the list (count going negative / stale) | mitigate | Optimistic list remove + fire-and-forget `unfollowSeller`; the server trigger (09-02) no-ops when the follow doc does not exist (before.exists == after.exists), preventing a negative counter (Pitfall 3). On Firestore failure the row re-appears (revert). |
| T-09-15 | Elevation | FollowedSellers list built for a signed-out user | mitigate | init reads `authRepository.currentUser.value?.uuid`; a blank uid yields an empty Room query (no cross-user data). The list is a personal, auth-scoped surface (D-03/D-04). |
| T-09-SC | Tampering (supply chain) | dependency installs | mitigate | No new libraries (09-UI-SPEC Registry Safety: only the already-on-classpath `Icons.Default.Group`). No install task. |
</threat_model>

<verification>
- `./gradlew :app:testDebugUnitTest --tests "*FollowedSellersViewModelTest" --tests "*SellerDashboardViewModelTest"` green (Task 1).
- `./gradlew :app:assembleDebug` green (Task 2 — screen + route + profile + seller surfaces compile).
- `./gradlew testDebugUnitTest` (all 3 modules) green as the wave-merge gate before verify-work.
- Device-gated / deferred: `./gradlew :app:connectedDebugAndroidTest --tests "*FollowedSellersScreenTest"`.
</verification>

<success_criteria>
- Followed Sellers list is reachable from a Profile row, loads from Room, supports optimistic unfollow, and rows navigate to the storefront (FAVS-02, D-04).
- Seller dashboard + profile show the live follower COUNT (never identities) and live aggregate rating, replacing hardcoded placeholders (FAVS-04, CD-04).
- ViewModel tests green; Compose UI test authored (device run deferred).
</success_criteria>

<output>
Create `.planning/phases/09-seller-storefronts-favorite-sellers/09-04-SUMMARY.md` when done.
</output>
