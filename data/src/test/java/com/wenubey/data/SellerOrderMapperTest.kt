package com.wenubey.data

import org.junit.Test

/**
 * Wave-0 scaffold. Real assertions land in Task 2 of Plan 06-01
 * (Entity <-> Domain JSON round-trip + corrupt-JSON fallback).
 */
class SellerOrderMapperTest {
    @Test
    fun `placeholder - JSON round-trip`() {
        // filled in Task 2
    }

    @Test
    fun `placeholder - corrupt JSON fallback`() {
        // filled in Task 2
    }
}
