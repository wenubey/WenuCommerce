package com.wenubey.domain

import org.junit.Test

/**
 * Wave-0 scaffold. The 5 sub-order combinations from RESEARCH §2.8 (all PENDING,
 * mixed SHIPPED+DELIVERED, hasCancelled mix, all CANCELLED, single CONFIRMED)
 * are exercised in onOrderStatusChange.test.ts (server-side mapping is the
 * source of truth). This class stays as a placeholder for any future client-side
 * aggregate helpers (e.g., for filter-chip "Partially Cancelled" handling).
 */
class AggregateStatusTest {
    @Test
    fun `placeholder - all PENDING aggregate`() {
        // future
    }

    @Test
    fun `placeholder - hasCancelled + mixed aggregate`() {
        // future
    }
}
