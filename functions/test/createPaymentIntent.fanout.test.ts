/**
 * createPaymentIntent fan-out tests — Phase 6 Order Tracking.
 *
 * Wave-0 scaffold. Real assertions land in Task 4 of Plan 06-01.
 */
describe("createPaymentIntent — seller fan-out", () => {
  it.todo("fans out 2 sellerOrders for a 2-seller cart");
  it.todo("allocates shipping pro-rata by per-seller subtotal");
  it.todo("applies single-seller coupon entirely to that seller");
  it.todo("rounding remainder lands on last seller");
});
